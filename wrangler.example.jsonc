/**
 * 🔐 TEMPLATE WRANGLER CONFIGURATION
 *
 * SECURITY NOTICE: This is a template file with placeholder values.
 *
 * INSTRUCTIONS:
 * 1. Copy this file to wrangler.jsonc: cp wrangler.example.jsonc wrangler.jsonc
 * 2. Replace ALL values marked with "TODO" with your actual values
 * 3. NEVER commit wrangler.jsonc to the repository (it's gitignored)
 *
 * For more details: https://developers.cloudflare.com/workers/wrangler/configuration/
 */
{
	"$schema": "node_modules/wrangler/config-schema.json",
	
	// TODO: Change this to your worker name (letters, numbers, hyphens only)
	"name": "your-contact-form",
	
	"main": "src/index.ts",
	
	// TODO: Replace with your Cloudflare Account ID
	// Get this by running: wrangler whoami
	// SECURITY: This is sensitive - never commit the real value
	"account_id": "00000000000000000000000000000000",
	
	"compatibility_date": "2025-06-28",
	"compatibility_flags": [
		"global_fetch_strictly_public",
		"nodejs_compat"
	],
	
	"observability": {
		"enabled": true
	},
	
	"d1_databases": [
		{
			"binding": "DB",
			// TODO: Change to your database name
			"database_name": "your-contact-db",
			// TODO: Replace with your D1 database ID
			// Get this by running: wrangler d1 create your-contact-db
			// SECURITY: This is sensitive - never commit the real value
			"database_id": "00000000-0000-0000-0000-000000000000"
		}
	],
	
	"vars": {
		// TODO: Change to your sending email address
		"FROM_EMAIL": "contact@yourdomain.com",
		
		// TODO: Change to where you want admin notifications sent
		"ADMIN_EMAIL": "admin@yourdomain.com",
		
		// TODO: Add your Mailgun domain (e.g., "mg.yourdomain.com")
		"MG_DOMAIN": "mg.yourdomain.com",
		
		// TODO: Add your Mailgun private API key
		// SECURITY: This is sensitive - never commit the real value
		"MG_API_KEY": "key-your-mailgun-api-key-here",
		
		"ENVIRONMENT": "production"
	}
	
	/**
	 * Optional Advanced Configuration
	 */
	
	// Smart Placement (recommended for global performance)
	// "placement": { "mode": "smart" },
	
	// Custom Domain (set up in Cloudflare Dashboard first)
	// "routes": [
	//   { "pattern": "contact.yourdomain.com/*", "custom_domain": true }
	// ],
	
	// Environment-specific variables
	// "env": {
	//   "staging": {
	//     "vars": {
	//       "ENVIRONMENT": "staging",
	//       "ADMIN_EMAIL": "staging-admin@yourdomain.com"
	//     }
	//   }
	// }
}