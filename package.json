{
	"name": "contact-form-admin-template",
	"version": "1.0.0",
	"private": true,
	"description": "Contact form and admin panel template for Cloudflare Workers",
	"keywords": ["cloudflare", "workers", "contact-form", "admin-panel", "d1", "email"],
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"build": "tsc"
	},
	"dependencies": {
		"mimetext": "^3.0.27"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.22.0"
	}
}
